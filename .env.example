# Frontend (Vite) environment variables
# Copy to .env or .env.local and fill in your project values
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side / Node scripts environment variables
# Used by admin scripts and service role operations (DO NOT expose service key to frontend)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=

# Notes:
# - The VITE_ prefix is required for variables to be exposed to Vite-built frontend code.
# - The SUPABASE_SERVICE_KEY is the service role key and must only be used on the server.
# - On Windows PowerShell, ensure your .env file is UTF-8 without BOM.